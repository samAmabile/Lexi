Custom Shell in C: Build a Unix-like shell in C (like sh) with support for piping, flags, and documentation. Build entirely in C and in one single file, with modules separated by comments. 
HTTP Server in C: A multi-threaded web server in C using sockets that can serve static HTML and handle GET/POST requests. Build entirely in C and in one single file, with modules separated by comments.
Memory Allocator in C: Implement your own malloc, free, and realloc using a linked list or buddy system. Build entirely in C and in one single file, with modules separated by comments.
Virtual Machine in C: Create a simple CPU emulator (like Chip-8 or a custom instruction set) with a stack and registers. Build entirely in C and in one single file, with modules separated by comments.
Relational DB in C: A basic SQLite-style database that uses a B-tree for indexing and handles simple SQL-like commands. Build entirely in C and in one single file, with modules separated by comments.
XoR Hashmap in C: Build a hashmap in C that maps (username, password) pairs to unique keys (e.g. {["username": "password"]: 208938923892} using XoR linked-list chaining for collisions. Enforce secure passwords. Include a CLI for creating usernames and paswords and generating keys to save them to the hashmap. Build entirely in C and in one single file, with modules separated by comments.
Chess Engine in C: Build a chess engine in C that works entirely in the terminal. Include user and computer roles. Generate a CLI type-based move inputting system and a simple board that renders in the terminal. Build entirely in C and in one single file, with modules separated by comments.
Tetris in C: Build a tetris analog in C that works entirely in the terminal/console. Build entirely in C and in one single file, with modules separated by comments.
Text Editor in C: Build a text editor in C using a gap buffer. Similar to vim or emacs. Build entirely in C and in one single file, with modules separated by comments.
BMP Image Processor in C: Write a C program that reads a .bmp file into memory, modifies the pixel data to convert the image to grayscale, and writes the result to a new file. Use a struct with #pragma pack(1) to map the BMP header. Build entirely in C and in one single file, with modules separated by comments.
Brainfuck Interpreter in C: Create a Brainfuck language interpreter in C. Use an array of 30,000 bytes as the memory tape and a switch statement inside a loop to process the eight standard commands: >, <, +, -, ., ,, [, and ]. Build entirely in C and in one single file, with modules separated by comments.
SQLite-style B-Tree in C: Implement a simple B-Tree indexing structure in C that stores integer keys. Write the core functions for insert_node and search_node, handling node splitting when a leaf exceeds a fixed degree. Build entirely in C and in one single file, with modules separated by comments.
Virtual CPU Emulator in C: Write a C emulator for a simple 8-bit CPU with a 16-bit address space. Define a struct for registers (A, X, Y, PC, SP) and a loop that fetches opcodes from a memory array and executes them. Build entirely in C and in one single file, with modules separated by comments.
Chat Application (TCP Sockets) in C: Develop a C chat server and client pair. The server should use select() or poll() to monitor multiple file descriptors and broadcast any message received from one client to all other connected clients.Build entirely in C and in one single file, with modules separated by comments.
Garbage Collector (Mark-and-Sweep) in C: Write a minimal 'Mark-and-Sweep' garbage collector in C. Create a function to register allocated pointers and a collect function that traverses a simulated root set to free unreachable memory.Build entirely in C and in one single file, with modules separated by comments.
DNS Server in C: Build a lightweight UDP DNS server that parses incoming queries, searches an in-memory cache or local zones file, and constructs valid DNS response packets. Build entirely in C and in one single file, with modules separated by comments.
BitTorrent Client in C: Create a console-based BitTorrent client that parses .torrent files, communicates with trackers via HTTP, and manages peer connections using non-blocking TCP sockets. Build entirely in C and in one single file, with modules separated by comments.
RSA Cryptosystem in C: Implement the RSA public-key cryptography algorithm from scratch, including prime number generation, modular exponentiation, and file encryption/decryption utilities. Build entirely in C and in one single file, with modules separated by comments.
Key-Value Store (LSM-Tree) in C: Build an on-disk Log-Structured Merge-tree key-value store featuring an in-memory MemTable, immutable SSTables, and a background compaction mechanism. Build entirely in C and in one single file, with modules separated by comments.
Cron Daemon in C: Create a system daemon in C that parses a crontab-like file, sleeps efficiently, and forks child processes to execute scheduled tasks based on time matching rules. Build entirely in C and in one single file, with modules separated by comments.
Tar Archiver in C: Write a utility from scratch that can pack multiple files and directories into a custom tar-like archive file format and extract them while preserving file permissions. Build entirely in C and in one single file, with modules separated by comments.
Regex Engine in C: Implement a regular expression compiler and matcher that converts regex strings into a Non-deterministic Finite Automaton (NFA) to evaluate text inputs. Build entirely in C and in one single file, with modules separated by comments.
ELF File Analyzer in C: Build a command-line tool that parses and Linkable Format (ELF) binaries to display header information, section headers, and the symbol table. Build entirely in C and in one single executable file, with modules separated by comments.
Redis Clone in C: Develop a single-threaded network key-value store using an event loop with epoll or select that supports basic commands like SET, GET, DEL, and EXPIRE. Build entirely in C and in one single file, with modules separated by comments.
LISP Interpreter in C: Write a tree-walking interpreter for a subset of the LISP programming language, featuring a dynamic environment, lexical scoping, and a read-eval-print loop (REPL). Build entirely in C and in one single file, with modules separated by comments.
WAV Audio Synthesizer in C: Create a programmatic audio tool that generates pure mathematical waveforms (sine, square, sawtooth), mixes them using an ADSR envelope, and outputs a valid .wav file. Build entirely in C and in one single file, with modules separated by comments.
Network Packet Sniffer in C: Write a command-line utility using raw sockets that intercepts network traffic, parses IPv4/IPv6 headers, and outputs real-time summaries of TCP, UDP, and ICMP packets. Build entirely in C and in one single file, with modules separated by comments.
Huffman File Compressor in C: Implement a file compression and decompression utility utilizing Huffman coding to construct dynamic frequency trees and output optimized binary payloads. Build entirely in C and in one single file, with modules separated by comments.
JSON Parser in C: Design a robust tokenizing JSON parser from scratch that converts a raw string into a structured C object hierarchy of objects, arrays, strings, and primitives. Build entirely in C and in one single file, with modules separated by comments.
Load Balancer in C: Develop a high-performance reverse-proxy load balancer using non-blocking sockets that distributes incoming HTTP traffic across backend instances via round-robin and least-connections. Build entirely in C and in one single file, with modules separated by comments.
Git Core Clone in C: Build a minimal version control tool that initializes repositories, hashes text files into a content-addressable object store, and manages commit objects via blobs. Build entirely in C and in one single file, with modules separated by comments.
Thread Pool Library in C: Implement a reusable, thread-safe task queue library using POSIX threads, mutexes, and condition variables to safely distribute arbitrary work to fixed worker groups. Build entirely in C and in one single file, with modules separated by comments.
Markdown to HTML Renderer in C: Write a text processor using a lexical state machine that parses Markdown text documents and outputs clean, syntactically correct HTML code structures. Build entirely in C and in one single file, with modules separated by comments.
GDB-like Debugger in C: Construct a basic process debugger using the ptrace system call that can launch target programs, set software breakpoints, step instructions, and inspect registers. Build entirely in C and in one single file, with modules separated by comments.
PNG Metadata Extractor in C: Create a tool that safely parses PNG file chunk structures (IHDR, PLTE, IDAT, IEND), validates CRC32 values, and extracts embedded textual metadata strings. Build entirely in C and in one single file, with modules separated by comments.
Base64 Encoder/Decoder in C: Develop an efficient binary-to-text converter that processes stream data bytes into Base64 standard characters and decodes them back with strict padding enforcement. Build entirely in C and in one single file, with modules separated by comments.
AES-128 Encryptor in C: Implement the full Advanced Encryption Standard (AES) 128-bit symmetric block cipher algorithm from scratch, including KeyExpansion, SubBytes, ShiftRows, MixColumns, and AddRoundKey. Build entirely in C and in one single file, with modules separated by comments.
Graph Pathfinding Visualizer in C: Build a terminal interface that simulates pathfinding grid mazes, computing shortest paths between coordinates using Dijkstra and A* algorithms with live step updates. Build entirely in C and in one single file, with modules separated by comments.
FAT16 File System Reader in C: Write a forensics tool that maps a raw FAT16 disk image file into memory structures to read boot sectors, parse directories, and extract individual files. Build entirely in C and in one single file, with modules separated by comments.
DHCP Server Mock in C: Construct a network service simulation that listens on UDP ports to receive discovery packets, manage IP address allocation pools, and hand out lease configurations. Build entirely in C and in one single file, with modules separated by comments.
Custom C Preprocessor in C: Design a text parsing engine that reads raw source code files to strip comments, expand simple macros, and handle recursive file inclusions properly. Build entirely in C and in one single file, with modules separated by comments.
FTP Client in C: Create a command-line File Transfer Protocol client that manages separate socket networks for control commands and active/passive data transfers to list and fetch assets. Build entirely in C and in one single file, with modules separated by comments.
Ray Caster Engine in C: Build a retro pseudo-3D game rendering loop in the terminal using ray casting techniques to project a 2D tile maze layout into a first-person perspective. Build entirely in C and in one single file, with modules separated by comments.
Diff Utility in C: Implement a text comparison tool that calculates line variations between two files utilizing the Longest Common Subsequence (LCS) dynamic programming algorithm. Build entirely in C and in one single file, with modules separated by comments.
Voxel Sandbox in C: Write a minimal terminal-based 3D environment engine that allows users to manipulate coordinate grids of blocks and dynamically re-renders vertical field lines on input. Build entirely in C and in one single file, with modules separated by comments.
SOCKS5 Proxy Server in C: Create an asynchronous proxy server supporting the SOCKS5 protocol that authenticates client requests and transparently forwards arbitrary TCP streams to destination servers. Build entirely in C and in one single file, with modules separated by comments.
Flight Telemetry Dashboard in C: Build a real-time command-line interface that reads mock flight text strings from a pipeline, updates attitude estimations, and draws a clean artificial horizon dashboard. Build entirely in C and in one single file, with modules separated by comments.
Assembler for Custom ISA in C: Write a text utility that converts raw assembly language strings into structured binary opcodes and operands for a simulated instruction set architecture. Build entirely in C and in one single file, with modules separated by comments.
Maze Generator and Solver in C: Design a programmatic grid tool that builds random non-trivial mazes using randomized Prim's algorithm and outputs the explicit solution sequence using depth-first search. Build entirely in C and in one single file, with modules separated by comments.
System Resource Monitor in C: Construct a live dashboard utilizing the Linux /proc pseudo-filesystem to parse, calculate, and refresh real-time CPU, RAM, process lists, and network load interfaces. Build entirely in C and in one single file, with modules separated by comments.
Basic BASIC Interpreter in C: Build a structural interpreter for a mini-BASIC dialect, executing numbered text instructions, maintaining simple variables, and evaluating expressions with operator precedence. Build entirely in C and in one single file, with modules separated by comments.
Consistent Hashing Ring in C: Implement a dynamic distributed storage assignment engine using a consistent hashing ring structure to handle node insertions and optimal key lookups seamlessly. Build entirely in C and in one single file, with modules separated by comments.
GIF Animator Deconstructor in C: Write an image file utility that parses multi-frame GIF bit streams into separate logical structures, unpacking embedded LZW compression data into raw pixel tables. Build entirely in C and in one single file, with modules separated by comments.
IP Address Subnet Calculator in C: Create a networking tool that takes an IP address and CIDR prefix block to compute network addresses, broadcast targets, host ranges, and subnet capacities. Build entirely in C and in one single file, with modules separated by comments.
Disassembler for x86 in C: Develop an parser that loads raw binary machine instructions and maps hex bytecode values back into readable human assembly language strings. Build entirely in C and in one single executable file, with modules separated by comments.
SMTP Mail Transfer Agent in C: Build a basic mail transmission system that connects to mail servers using raw TCP sockets, executing standard HELO, MAIL FROM, RCPT TO, and DATA workflows. Build entirely in C and in one single file, with modules separated by comments.
Game of Life Simulator in C: Write an optimized cellular automata frame loop that tracks grid states, computes generation state changes via game rules, and renders smooth terminal animations. Build entirely in C and in one single file, with modules separated by comments.
Message Broker (Pub/Sub) in C: Create a centralized chat/event network service engine that registers topic listeners and broadcasts asynchronous data updates to subscribing client sockets. Build entirely in C and in one single file, with modules separated by comments.
Vector Graphics Core in C: Implement a software-based canvas engine that calculates vector paths to draw straight lines, bezier curves, and text fonts into uncompressed memory pixel buffers. Build entirely in C and in one single file, with modules separated by comments.
In-Memory Cache (LRU) in C: Build a fast key-value buffer structure combining a hashmap with a doubly linked list to achieve strict O(1) Least Recently Used cache evictions. Build entirely in C and in one single file, with modules separated by comments.
Password Hash Cracker in C: Write a multi-threaded security testing utility that processes wordlists, runs candidate strings through cryptographic hashing loops, and identifies matching hash inputs. Build entirely in C and in one single file, with modules separated by comments.
Snake Game in C: Create the retro arcade game loop entirely inside the console workspace using non-blocking keyboard input loops to update grid item positions and length scores. Build entirely in C and in one single file, with modules separated by comments.
CSV Data Analyzer in C: Design a data frame parser that reads massive arbitrary comma-separated files, dynamically handles field typing, and computes fast columns, averages, and variance metrics. Build entirely in C and in one single file, with modules separated by comments.
Lexical Tokenizer in C: Implement a stateful lexical analysis tool that ingests structural application text files and translates raw sequences into a clean token stream for compilation. Build entirely in C and in one single file, with modules separated by comments.
Syslog Daemon Clone in C: Construct a network logging service that listens for local and remote UDP message payloads, filtering and appending incoming system alerts to distinct log structures. Build entirely in C and in one single file, with modules separated by comments.
Space Invaders Clone in C: Build a retro terminal-based arcade experience managing entity vectors for spaceships, descending alien shields, and projectile collision check loops in realtime. Build entirely in C and in one single file, with modules separated by comments.
Arithmetic Expression Parser in C: Write an evaluation engine that transforms complex math strings into an Abstract Syntax Tree using the Shunting-Yard algorithm to execute structural calculations. Build entirely in C and in one single file, with modules separated by comments.
Event-Driven HTTP Client in C: Create a non-blocking download engine that initializes concurrent raw TCP requests, manages connection timeouts, and processes chunked web transfers efficiently. Build entirely in C and in one single file, with modules separated by comments.
Custom File System in User Space in C: Implement a virtual file tracking architecture using a mock disk file block array to create directory listings, read, write, and map files. Build entirely in C and in one single file, with modules separated by comments.
Ping Utility in C: Build a diagnostic networking tool using raw sockets to construct, transmit, and track low-level ICMP Echo Requests and process returning target round-trip times. Build entirely in C and in one single file, with modules separated by comments.
Matrix Multiplication Optimization in C: Write a scientific data matrix utility implementing block optimization, loop unrolling, and multithreading strategies to maximize raw floating-point computation speeds. Build entirely in C and in one single file, with modules separated by comments.
Bloom Filter in C: Create a probabilistic set membership system using independent hash computations over a mutable bit vector array to yield rapid, memory-efficient existence searches. Build entirely in C and in one single file, with modules separated by comments.
Command Line Arguments Parser in C: Design a comprehensive argument validation engine that processes arbitrary shell flags, short/long switches, type conversions, and errors gracefully. Build entirely in C and in one single file, with modules separated by comments.
Virtual Terminal Interface (TUI) in C: Write a core screen rendering manager that utilizes ANSI escape sequences to establish sub-windows, text fields, menu highlights, and borders in raw terminals. Build entirely in C and in one single file, with modules separated by comments.
UUID Generator in C: Implement a unique identifier generator that produces standard conforming RFC 4122 Version 4 random tracking strings utilizing random byte pools. Build entirely in C and in one single file, with modules separated by comments.
SQLite Database Reader in C: Build a read-only parsing tool that connects directly to a real binary SQLite file, reading the master schemas and traversing interior page B-trees. Build entirely in C and in one single file, with modules separated by comments.
Keylogger Simulation in C: Write a low-level diagnostic input event loop that hooks local keyboard activities on Linux/POSIX devices, translating raw scanning codes into characters. Build entirely in C and in one single file, with modules separated by comments.
URL Parser in C: Construct a rigorous text splitting utility that breaks arbitrary network location strings down into schemes, credentials, host addresses, port targets, paths, and query fragments. Build entirely in C and in one single file, with modules separated by comments.
B-Plus Tree Index in C: Implement an on-disk B+ Tree database indexing structure where interior nodes hold routing indices and bottom leaf lists link records sequentially for scanning. Build entirely in C and in one single file, with modules separated by comments.
WebSockets Server in C: Create an HTTP connection server that handles handshake protocol upgrades and manages full-duplex binary framing communications streams over long-lived sockets. Build entirely in C and in one single file, with modules separated by comments.
Digital Clock and Timer in C: Write a console dashboard that reads system time structures to render dynamic, retro ASCII-art multi-segment number layouts across the shell display screen. Build entirely in C and in one single file, with modules separated by comments.
Archive Extractor (.zip) in C: Develop an unzipping utility that reads ZIP local file headers, navigates central directories, and decompresses DEFLATE data blocks using standard decompression algorithms. Build entirely in C and in one single file, with modules separated by comments.
String Search Engine in C: Implement a pattern finder suite comparing text matching execution speeds across Boyer-Moore, Knuth-Morris-Pratt, and Rabin-Karp substring searching algorithms. Build entirely in C and in one single file, with modules separated by comments.
Process Scheduler Simulation in C: Create an operating system task manager simulator evaluating workload executions against First-Come-First-Served, Shortest-Job-First, and Round-Robin scheduling queues. Build entirely in C and in one single file, with modules separated by comments.
Dynamic String Library in C: Design a safe, drop-in replacement for standard C-strings that maintains tracking capacities, automates memory expansions, and prevents buffer overflow mutations. Build entirely in C and in one single file, with modules separated by comments.
DNA Sequence Matcher in C: Write a bioinformatics search utility that aligns long genetic string chains using the Needleman-Wunsch algorithm to output score matrices and sequence variations. Build entirely in C and in one single file, with modules separated by comments.
Audio Waveform Player in C: Build a raw file parser that feeds digitized sample tracks from standard linear audio containers into physical system speaker buffers using ALSA drivers. Build entirely in C and in one single file, with modules separated by comments.
Port Scanner in C: Create a multi-threaded reconnaissance tool that maps target connection grids rapidly using asynchronous TCP connect and half-open SYN scanning mechanics. Build entirely in C and in one single file, with modules separated by comments.
Secure File Eraser in C: Develop a secure shredding program that overwrites target disk sectors with randomized patterns, zeroes, and compliance schemas before severing system file handles. Build entirely in C and in one single file, with modules separated by comments.
Text-Based Rogue-like Game in C: Build a random dungeon crawling adventure engine in the terminal featuring dynamic map procedural generation, field of view mapping, and turn-based hostile pathing. Build entirely in C and in one single file, with modules separated by comments.
XML Parser in C: Create a robust tree-building XML reader that checks for well-formed nested tag tags, attributes, and text nodes, outputting an accessible Document Object Model (DOM). Build entirely in C and in one single file, with modules separated by comments.
Linear Algebra Library in C: Implement a math matrix toolkit supporting multi-dimensional conversions, determinant computations, matrix inversions, and eigen value calculations safely from scratch. Build entirely in C and in one single file, with modules separated by comments.
IP Traffic Monitor in C: Write an interface aggregation application tracking data totals, packet counts, and bandwidth speeds per internal network IP port address using raw interface queries. Build entirely in C and in one single file, with modules separated by comments.
Markdown Editor in C: Build a splitscreen terminal application that lets users modify raw text files in a left panel while viewing live formatted output lines on the right side. Build entirely in C and in one single file, with modules separated by comments.
Virtual Memory Paging Simulator in C: Create a hardware architecture simulator that manages virtual page tables, tracks frame translations, and handles page fault evictions via FIFO and LRU filters. Build entirely in C and in one single file, with modules separated by comments.
Token Bucket Rate Limiter in C: Implement a multi-threaded network throttling engine using standard clocks to accumulate tracking credits, rejecting incoming request threads that exceed constraints. Build entirely in C and in one single file, with modules separated by comments.
Base32 Converter in C: Write a text encoding tool conforming to RFC 4648 that groups raw binary bit arrays into five-bit values to yield structured string layouts. Build entirely in C and in one single file, with modules separated by comments.
TLS Handshake Parser in C: Design a network protocol diagnostic analyzer that intercepts client-server greetings, extracts supported cipher suites, and verifies security version properties. Build entirely in C and in one single file, with modules separated by comments.
Arithmetic Encoder in C: Build a data compression utility that tracks context probabilities to encode entire file contents into fine fractional range values across a single decimal scale. Build entirely in C and in one single file, with modules separated by comments.
Log Aggregator Server in C: Create a concurrent TCP storage daemon that safely flushes mixed application event updates into structured multi-indexed thread-safe operational databases. Build entirely in C and in one single file, with modules separated by comments.
PCX Image Loader in C: Write an old-school image tool that reads ZSoft PCX layouts, handles low-level Run-Length Encoding (RLE) un-packing loops, and converts color palettes to raw RGB. Build entirely in C and in one single file, with modules separated by comments.
Advanced Math Expression Calculator in C: Build a console interpreter supporting custom variable assignments, custom algebraic function creation, and recursive mathematical evaluations using a custom syntax parser. Build entirely in C and in one single file, with modules separated by comments.
Object Pool Pattern Allocator in C: Design a highly optimized system memory framework that creates fixed-size blocks upfront to hand out and reclaim recycled structures without standard heap overhead. Build entirely in C and in one single file, with modules separated by comments.
Terminal Screen Recorder in C: Write an administrative capturing program that mirrors active pseudo-terminal descriptor lines, saving session inputs, layout mutations, and timetables into file archives. Build entirely in C and in one single file, with modules separated by comments.
Netcat Clone in C: Build a networking Swiss-Army knife utility that transfers arbitrary raw file streams or establishes raw backend terminal bridges across custom TCP or UDP links. Build entirely in C and in one single file, with modules separated by comments.
Astrodynamics Calculator in C: Create a physics tool that integrates orbit tracking algorithms, calculating Keplerian orbit states, planetary body trajectories, and transfer delta-v requirements over time scales. Build entirely in C and in one single file, with modules separated by comments.
Graph Database Engine in C: Implement a localized database schema that explicitly links relational entities using dynamic pointer lists to evaluate network relationships and fast graph paths. Build entirely in C and in one single file, with modules separated by comments.
Soundex Name Classifier in C: Design an phonetic indexing string library that normalizes words into character-code patterns to find matching surnames despite phonetic spelling variations. Build entirely in C and in one single file, with modules separated by comments.
INI Configuration Parser in C: Write a lean file analyzer that processes configurations, stripping whitespace to populate accessible runtime key-value maps grouped inside section definitions. Build entirely in C and in one single file, with modules separated by comments.
Network Traffic Generator in C: Create a load testing program capable of generating customized distributions of synthetic TCP/UDP packet bursts to gauge network throughput limits under stress. Build entirely in C and in one single file, with modules separated by comments.
Conway’s Game of Life (Infinite Grid) in C: Build a cellular simulation using dynamic hash tables to store only living coordinate spaces, supporting growing patterns without fixed border limits. Build entirely in C and in one single file, with modules separated by comments.
Vector Library in C: Create a generic, resizable sequence structure mimicking array lists that automatically doubles internal capacity allocations and handles safe deletions and insertions. Build entirely in C and in one single file, with modules separated by comments.
Base85 Document Encoder in C: Implement a text compression conversion system that groups four bytes of binary inputs into five ASCII printable signs to streamline data exchanges. Build entirely in C and in one single file, with modules separated by comments.
Custom Bitstream Writer in C: Design an input-output utility that lets programs serialize fractional variable widths (like 3-bit or 11-bit integers) packed into unaligned target output arrays. Build entirely in C and in one single file, with modules separated by comments.
Terminal Text Art Editor in C: Build an interactive terminal canvas that accepts pointer or keyboard motions to draw custom multi-colored character designs and outputs raw ANSI string frames. Build entirely in C and in one single file, with modules separated by comments.
Sudoku Solver & Generator in C: Create a grid program that constructs complex, unique puzzles using backtracking algorithms and returns exact step-by-step matrix solutions instantly upon request. Build entirely in C and in one single file, with modules separated by comments.
TGA Image Filter in C: Write an image utility that loads uncompressed Truevision TGA files, applies mathematical convolution matrix filters (sharpen, blur, edge detect), and writes output images. Build entirely in C and in one single file, with modules separated by comments.
Cryptographic Random Number Engine in C: Implement a secure seed accumulator system that reads entropy hardware states to output unpredictable, high-quality pseudo-random byte loops for key generation. Build entirely in C and in one single file, with modules separated by comments.
Mini Compiler for Arithmetic in C: Build a full pipeline parser that tracks math strings, isolates tokens, builds an intermediate representation tree, and prints actual assembly instructions. Build entirely in C and in one single file, with modules separated by comments.
Virtual Disk Drive Driver in C: Construct a program that establishes a virtual storage block space in a single flat file, mounting, creating directories, and validating sector read-write calls. Build entirely in C and in one single file, with modules separated by comments.
Key-Value Store (Hash Table) in C: Build an in-memory key-value engine using dynamic resizing arrays with Robin Hood hashing collision resolutions to ensure predictable search operations. Build entirely in C and in one single file, with modules separated by comments.
Ray Tracer in C: Implement an offline 3D rendering tool that shoots light vectors into abstract mathematical scenes to compute sphere intersections, mirror reflections, and point shadows. Build entirely in C and in one single file, with modules separated by comments.
Intellectual Property Obfuscator in C: Create a code management tool that loads clean source files and transforms variable identifiers, strips layouts, and alters expressions to hide structural intents. Build entirely in C and in one single file, with modules separated by comments.
TF-IDF Text Indexer in C: Write a search analysis utility that reviews a directory of files, builds term-frequency indexing summaries, and evaluates search query relevance scores. Build entirely in C and in one single file, with modules separated by comments.
Simple Network Time Protocol (SNTP) Client in C: Create an internet synchronization utility that sends time requests to NTP servers via UDP, processes timestamp formats, and corrects local system clocks. Build entirely in C and in one single file, with modules separated by comments.
Assembly Instruction Simulator in C: Write a virtual architecture environment that tracks an explicitly defined assembly language, adjusting registers and evaluating jumps inside custom code lines. Build entirely in C and in one single file, with modules separated by comments.
HTML Sanitizer in C: Design a text safety processing engine that parses raw internet pages, identifying and removing nested scripting tags or harmful attributes while saving text layouts. Build entirely in C and in one single file, with modules separated by comments.
Huffman Stream Encoder in C: Build a real-time data utility that takes continuous microphone or file streams, encoding values on-the-fly into optimized compressed bit representations. Build entirely in C and in one single file, with modules separated by comments.
Task Graph Scheduler in C: Implement an execution architecture that reads operational dependency structures, builds an execution chart, and fires independent multi-threaded tasks safely. Build entirely in C and in one single file, with modules separated by comments.
Custom Memory Allocator (Slab) in C: Design an optimized memory engine that partitions uniform kernel pages into item caches to handle frequent allocations of matching struct objects. Build entirely in C and in one single file, with modules separated by comments.
Unix `grep` Clone in C: Write a command-line search utility that tracks file texts, matches line elements using custom structural expression strings, and outputs colorized terminal highlights. Build entirely in C and in one single file, with modules separated by comments.
Hex Editor in C: Build an interactive console utility that displays raw file byte charts alongside translated text lines, allowing inline hex updates and file rewrites. Build entirely in C and in one single file, with modules separated by comments.
Advanced Network Vulnerability Scanner in C: Create a diagnostics utility that sends malformed TCP packets to open target ports, reviewing response signatures to infer operating system profiles. Build entirely in C and in one single file, with modules separated by comments.
Bit-Packed Chess Board in C: Implement a chess data layer representing entire field states inside discrete 64-bit integer bitboards to evaluate valid moves using fast binary masks. Build entirely in C and in one single file, with modules separated by comments.
Custom RPC System in C: Build a remote procedure call framework that converts execution structures into binary buffers to trigger processes across network sockets and parse responses. Build entirely in C and in one single file, with modules separated by comments.
Tar Compression Decoder in C: Create an archive engine that loads uncompressed tar files, walks file headers, maps structural permissions, and builds local folders to extract nested objects. Build entirely in C and in one single file, with modules separated by comments.
Bytecode Virtual Machine in C: Write a stack-based instruction runtime environment that interprets compiled hex execution files, tracking data adjustments across isolated virtual memory pools. Build entirely in C and in one single file, with modules separated by comments.
SSH Client Connection Framework in C: Design a network utility that initiates socket channels, manages protocol negotiations, and validates secure server key authentication steps manually. Build entirely in C and in one single file, with modules separated by comments.
Dynamic Linking Loader in C: Construct a low-level binary utility that reads raw shared object library files into memory addresses, fixes offset references, and triggers target functions. Build entirely in C and in one single file, with modules separated by comments.
Unix `find` Clone in C: Build a directory exploration tool that searches file hierarchies recursively, filtering listings by filename rules, modification dates, size limits, and access constraints. Build entirely in C and in one single file, with modules separated by comments.
Huffman Tree Archiver in C: Write a multi-file backup system that hashes data streams, builds uniform prefix maps, and combines compressed files alongside decoding data tables into individual files. Build entirely in C and in one single file, with modules separated by comments.
Lexer Generator in C: Design an automation framework that processes character definitions and outputs customized functional tokenizing state machines tailored for unique code structures. Build entirely in C and in one single file, with modules separated by comments.
Calendar Engine in C: Create a console utility that accepts year and month parameters, computes weekday distributions using Zeller's Congruence, and prints clean calendar layouts. Build entirely in C and in one single file, with modules separated by comments.
IPC Message Bus in C: Implement a centralized local routing application that forwards dynamic structural communications between separate system processes using shared memory spaces and semaphores. Build entirely in C and in one single file, with modules separated by comments.
Packet Sniffer Filters in C: Create a network diagnostic utility that compiles text filter rules into optimized bytecode masks to evaluate and capture specific streaming socket payloads. Build entirely in C and in one single file, with modules separated by comments.
Math Equation Simplifier in C: Build an algebraic text tool that breaks equations into abstract tree tokens to locate, reduce, and output simplified variants of matching mathematical structures. Build entirely in C and in one single file, with modules separated by comments.
Thread-Safe Logging Utility in C: Design an enterprise tracking framework that aggregates multi-threaded message lines, ensuring sequential non-blocking database writes using atomic locks and ring buffers. Build entirely in C and in one single file, with modules separated by comments.
RC4 Stream Cipher in C: Implement the full Rivest Cipher 4 symmetric encryption utility from scratch, building the key-scheduling algorithm (KSA) and pseudo-random generation algorithm (PRGA) stream loops. Build entirely in C and in one single file, with modules separated by comments.
Text-Based Tycoon Game in C: Build a business strategy simulation engine in the terminal that models financial balance sheets, tracks asset depreciation, and processes market variables over time. Build entirely in C and in one single file, with modules separated by comments.
Custom Stack Unwinder in C: Write a low-level error debugging tool that steps through active CPU frame pointer addresses to print execution histories during application crashes. Build entirely in C and in one single file, with modules separated by comments.
Fast Fourier Transform (FFT) Core in C: Implement the Cooley-Tukey algorithm to calculate fast conversions of discrete audio or signal sample loops into distinct frequency spectrum arrays. Build entirely in C and in one single file, with modules separated by comments.
IP Routing Table Engine in C: Create a network mapping tool that uses a trie data structure to store route pathways and compute optimal longest-prefix matches for destination IPs. Build entirely in C and in one single file, with modules separated by comments.
Plagiarism Checker in C: Write a document auditing utility that converts text inputs into numerical hash sequences to calculate matching content similarities between source files. Build entirely in C and in one single file, with modules separated by comments.
Custom Bencode Parser in C: Design a data parsing utility that reads the strict serialization layout used by BitTorrent, handling integers, strings, nested lists, and sorted dictionaries. Build entirely in C and in one single file, with modules separated by comments.
Terminal-Based Video Player in C: Build a media display tool that loads raw video data blocks, downsamples pixel tables to match terminal grids, and outputs colorized ANSI characters smoothly. Build entirely in C and in one single file, with modules separated by comments.
Multi-Threaded Port Scanner in C: Create an advanced network tool that allocates variable port ranges across thread groups to map active connection openings on destination machines. Build entirely in C and in one single file, with modules separated by comments.
LZ77 File Compressor in C: Write a text optimization program implementing sliding-window dictionary matching loops to substitute recurring string segments with compact backward offset tracking pairs. Build entirely in C and in one single file, with modules separated by comments.
Custom Memory Allocator (Slab-Buddy Hybrid) in C: Build an advanced allocator using a buddy system for large page requests and a slab layout to manage small item allocations. Build entirely in C and in one single file, with modules separated by comments.
Secure File Transfer Protocol Server in C: Create a network service daemon that parses authentication tokens and runs encrypted command steps to move files between system endpoints securely. Build entirely in C and in one single file, with modules separated by comments.
Terminal Waveform Visualizer in C: Design a real-time signal monitor that captures microphone input feeds via audio APIs and draws responsive amplitude graphics using console vertical bars. Build entirely in C and in one single file, with modules separated by comments.
Custom Assembly Language VM in C: Build an emulation environment featuring a custom compiler that processes simple text instructions into bytecode arrays and steps through execution routines. Build entirely in C and in one single file, with modules separated by comments.
Mini Relational DB (SQL Parser) in C: Write a tokenizing query module that scans incoming SQL text strings, identifies keywords, and builds structured operations targets for backend engines. Build entirely in C and in one single file, with modules separated by comments.
Unix `top` Clone in C: Construct a system administration dashboard that loops continuously to map active process nodes, tracking thread loads, process states, and real-time CPU consumption. Build entirely in C and in one single file, with modules separated by comments.
In-Memory Cache (LFU) in C: Build an optimization key-value engine that maintains frequency counters over stored entries to drop Least Frequently Used objects during buffer capacity limits. Build entirely in C and in one single file, with modules separated by comments.
Markdown-to-PDF Core Generator in C: Create a documentation converter that reads structural Markdown texts, translates paragraph layout tags, and encodes properly styled binary PDF text files from scratch. Build entirely in C and in one single file, with modules separated by comments.
